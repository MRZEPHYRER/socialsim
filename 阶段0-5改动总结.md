# 阶段 0-5 改动总结

本文档简要记录本轮对项目完成的五个阶段改动，以及当前模型语义和验收方式。

## 阶段 0：诊断与守恒检查

新增了按 step 记录的诊断体系，用于检查模型核心守恒关系。

已覆盖的检查包括：

- 商品守恒：企业库存、央行库存、产出、销售、救济、损耗之间是否闭合。
- 货币核算：初始私有现金、净货币创造、家庭现金、企业现金、公共财富、央行公共收入余额是否闭合。
- 货币变动：每步 located money stock 的变化是否等于央行/账本记录的净货币创造。
- 收入支出对应：家庭消费支出与市场销售收入是否一致。
- 销售收入拆分：市场销售收入是否等于企业收入加央行释放库存收入。
- 账本净货币变化：ledger 记录的创造/销毁是否等于模型净货币变化。

入口侧支持：

- `--seed`
- `--diagnostics-csv`
- `--no-diagnostics-csv`
- `--ledger-csv`
- `--no-ledger-csv`
- `--progress-interval`

默认运行会输出 diagnostics summary，并可导出 CSV。

## 阶段 1：最低需要系统解耦

新增 `NeedsSystem`，把“家庭结构和年龄决定最低生活需要”从消费购买逻辑中分离出来。

当前语义：

- `NeedsSystem.household_minimum_need_units()` 负责计算最低需要单位。
- `NeedsSystem.household_minimum_cost()` 负责将最低需要乘以价格指数。
- 旧的 `ConsumptionSystem.household_necessary_consumption()` 保留兼容入口，但内部委托给 `NeedsSystem`。
- 企业消费市场通过 `NeedsSystem` 获取家庭最低需要。

这样后续可以更自然地扩展多商品最低需要，而不把家庭人口结构逻辑混在购买行为里。

## 阶段 2：通用商品壳

新增 `GoodSpec` / `GoodsCatalog`，目前只有一个基础消费品：

- `good_id = "basic_consumption_good"`
- 显示名仍可理解为 food / basic consumption good。

当前设计保留了旧字段名里的 `food_`，以减少一次性改动范围；但内部已经有 `good_id` 和商品目录，后续可以逐步把 food-specific 逻辑迁移成多商品通用逻辑。

## 阶段 3：统一账本

新增统一 ledger 接口，所有主要资金变化尽量通过账本表达：

- `ledger.transfer(...)`
- `ledger.create_money(...)`
- `ledger.destroy_money(...)`
- `ledger.create_loan(...)`
- `ledger.repay_loan(...)`

同时为了性能补了 attr 快速路径：

- `transfer_attrs`
- `create_money_attr`
- `destroy_money_attr`
- `create_loan_attr`
- `repay_loan_attrs`

账本现在可以记录：

- 谁付钱
- 谁收钱
- 金额
- 原因
- 是否创造货币
- 是否销毁货币
- 是否创建贷款
- 是否偿还贷款

性能处理：

- 默认不记录逐笔 ledger CSV，只记录每步摘要。
- 只有传入 `--ledger-csv` 时才开启详细逐笔记录。
- 工资、分红、家庭购买等高频路径在非详细账本模式下做了聚合记录，避免 5000 步运行过慢。

## 阶段 4：目标财富消费

家庭消费拆成两层：

- 最低生活消费：`minimum_cost`
- 可选消费：`optional_consumption`

新增目标财富逻辑：

```text
target_wealth = reserve_months * minimum_cost
wealth_gap = target_wealth - starting_wealth
```

当前行为：

- 家庭低于目标财富时，提高储蓄倾向，降低可选消费乘数。
- 家庭高于目标财富时，允许一小部分超额财富转化为可选消费。
- 最低生活消费不会因为“想储蓄”被主动削掉。
- 如果家庭实际买不起最低生活需要，仍可能因为预算约束产生不足，但这不是目标财富规则主动压低的结果。

相关诊断字段包括：

- `average_target_wealth`
- `average_wealth_gap`
- `total_optional_consumption`
- `average_optional_consumption_multiplier`

## 阶段 5：企业贷款替代公共采购主发行

引入企业流动资金贷款循环。当前 `CentralBank` 暂时兼具信贷银行功能，未来可拆出商业银行。

当前流程：

1. 企业生产/发工资前检查现金是否低于工资需求和现金缓冲。
2. 若现金不足，向央行申请流动资金贷款。
3. 贷款通过 ledger 创造货币，并增加企业现金。
4. 企业发工资，家庭消费，企业获得销售收入。
5. 企业销售回款后，若现金超过还款缓冲，则偿还部分本金。
6. 偿还本金通过 ledger 销毁货币，并降低贷款余额。

公共采购的角色调整：

- 默认关闭库存采购直接新发货币。
- 央行库存采购默认只使用 `public_income_balance`。
- 因此公共采购退回“战略储备/救济工具”，不再是企业现金托底器。

重要修正：

- 央行卖出公共收入购买的库存时，不再销毁货币，而是把收入转回 `central_bank.public_income_balance`。
- 只有当央行库存来自新发货币购买时，卖出对应库存收入才销毁货币。

这修复了此前 `money supply stock` 近似线性下跌甚至变负的问题。

## 当前 money supply 口径

报告里的 money supply 更准确地说是：

```text
相对于初始货币存量的净新增货币 / 未偿信用货币
```

默认情况下企业初始现金充足，不需要贷款；公共采购也不再新发货币，所以该值长期维持在 0 是正常的。

真正的总现金存量应看：

- `Initial Private Cash Stock`
- `Final Expected Money Stock`
- `Final Located Money Stock`
- `Final Private Cash Stock`

若将企业初始现金设低，贷款机制会触发，此时：

- 贷款发放会提高 money supply。
- 本金偿还会降低 money supply。
- 期末 money supply 应接近未偿贷款余额。

## 性能优化

为保证 5000 步可运行，额外做了若干性能优化：

- active households 缓存。
- 诊断统计合并扫描。
- 家庭最低需要使用 `person_dict` 哈希查找。
- 死亡和家庭清理减少全表检索。
- 生育系统缓存年龄生育倍率、子女成本和 parity 因子。
- 默认关闭详细账本逐笔记录。

当前用户实测 5000 步约 7 分钟，已比此前明显改善。

## 验收方式

常规运行：

```powershell
python main.py
```

短测试：

```powershell
python main.py --population 1000 --steps 80 --progress-interval 0 --no-plots
```

性能测试：

```powershell
python main.py --population 5000 --steps 200 --progress-interval 0 --no-plots --no-diagnostics-csv
```

详细账本审计：

```powershell
python main.py --population 500 --steps 20 --progress-interval 0 --no-plots --ledger-csv test/output/ledger.csv
```

重点看：

- `Invariant violations: 0`
- `Max abs monetary accounting gap`
- `Max abs money delta gap`
- `Max abs ledger money net gap`
- `Max abs food conservation gap`
- `Max abs income spending gap`
- `Max abs sales revenue split gap`

## 后续可思考方向

- 把报告里的 money supply 名称进一步改成 `Net New Money` 或 `Credit Money Outstanding`，避免和总货币存量混淆。
- 把 `CentralBank` 的信贷功能拆成临时 `CreditBank` 或未来的 `CommercialBank`。
- 在多商品扩展前，进一步抽象库存、需求、价格和市场结算接口。
- 重新校准企业初始现金和贷款触发参数，让贷款机制更早、更自然地成为主发行机制。
- 增加专门的低现金/贷款循环测试用例，固定验证贷款发放、消费回流、还本销毁之间的闭环。
